<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/WHITE"
    tools:context="com.ass3.axue2.posapplication.activities.Settings.SettingsActivity">

    <Button
        android:id="@+id/settings_restaurant_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/WHITE"
        android:gravity="start|center_vertical"
        android:paddingEnd="10dp"
        android:paddingStart="10dp"
        android:stateListAnimator="@null"
        android:text="@string/restaurant" />

    <Button
        android:id="@+id/settings_tables_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/WHITE"
        android:gravity="start|center_vertical"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:text="@string/tables"
        android:stateListAnimator="@null"/>

    <Button
        android:id="@+id/settings_groups_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/WHITE"
        android:gravity="start|center_vertical"
        android:paddingEnd="10dp"
        android:paddingStart="10dp"
        android:stateListAnimator="@null"
        android:text="@string/groups" />

    <Button
        android:id="@+id/settings_products_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/WHITE"
        android:gravity="start|center_vertical"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:text="@string/products"
        android:stateListAnimator="@null"/>

    <Button
        android:id="@+id/settings_database_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/WHITE"
        android:gravity="start|center_vertical"
        android:paddingEnd="10dp"
        android:paddingStart="10dp"
        android:stateListAnimator="@null"
        android:text="@string/database" />

    <Button
        android:id="@+id/settings_network_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/WHITE"
        android:gravity="start|center_vertical"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:text="@string/network"
        android:stateListAnimator="@null"/>

</LinearLayout>
